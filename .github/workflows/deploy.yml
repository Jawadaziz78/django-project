name: Deployment Pipeline

on:
  push:
    branches: [ "development", "stage", "test" ]

env:
  PROJECT_TYPE: 'laravel'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Execute Pipeline
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: PROJECT_TYPE
          script: |
            set -e
            BRANCH=${{ github.ref_name }}
            
            # 1. INITIALIZATION
            echo "--- 🚀 Starting Deployment for $BRANCH branch ---"
            cd /var/www/html/$BRANCH/$PROJECT_TYPE-project
            git pull origin $BRANCH
            echo "✅ Initialization and Git Pull: DONE"

            # 2. SONARQUBE ANALYSIS
            echo "--- 🔍 Running SonarQube Analysis ---"
            export SONAR_NODE_ARGS='--max-old-space-size=512'
            /home/ubuntu/sonar-scanner/bin/sonar-scanner \
              -Dsonar.projectKey=${PROJECT_TYPE}-project \
              -Dsonar.sources=app \
              -Dsonar.inclusions=**/*.php \
              -Dsonar.host.url=${{ secrets.SONAR_HOST_URL }} \
              -Dsonar.login=${{ secrets.SONAR_TOKEN }}
            echo "✅ SonarQube Analysis: DONE"

            # 3. QUALITY GATE
            echo "--- 🚦 Checking Quality Gate Status ---"
            sleep 10
            for i in {1..24}; do
              STATUS=$(curl -s -u ${{ secrets.SONAR_TOKEN }}: "${{ secrets.SONAR_HOST_URL }}/api/qualitygates/project_status?projectKey=${PROJECT_TYPE}-project" | jq -r '.projectStatus.status')
              if [ "$STATUS" = "OK" ] || [ "$STATUS" = "ERROR" ]; then break; fi
              echo "Current Status: $STATUS (waiting...)"
              sleep 5
            done
            echo "✅ Quality Gate Check: DONE (Status: $STATUS)"

            # 4. SAFETY CHECK
            if [ "$STATUS" != "OK" ]; then 
              echo "❌ ERROR: Quality Gate failed. Aborting deployment."
              exit 1 
            fi

            # 5. BUILD AND DEPLOY
            echo "--- 🛠 Starting Build and Deploy for $PROJECT_TYPE ---"
            case "$PROJECT_TYPE" in
                vue) 
                  VITE_BASE_URL="/$PROJECT_TYPE/$BRANCH/" 
                  BASE_URL="/$PROJECT_TYPE/$BRANCH/" 
                  npm install && npm run build 
                  ;;
                nextjs) 
                  npm install && npm run build 
                  pm2 restart $PROJECT_TYPE-$BRANCH 
                  ;;
                laravel) 
                  composer install --no-interaction --prefer-dist --optimize-autoloader
                  php artisan optimize:clear
                  php artisan storage:link
                  sudo chown -R ubuntu:www-data storage bootstrap/cache public
                  sudo chmod -R 775 storage bootstrap/cache
                  ;;
            esac
            echo "✅ Build and Deploy: DONE"
            echo "--- 🎉 Pipeline Execution Finished Successfully ---"

  # ========================================================================
  # SLACK NOTIFICATION (Commented Out)
  # ========================================================================
  # notify:
  #   needs: deploy
  #   if: always()
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Send Slack Notification
  #       run: |
  #         if [ "${{ needs.deploy.result }}" == "success" ]; then
  #           TEXT="✅ *Deployment Successful*\n📂 Project: ${{ env.PROJECT_TYPE }}\n🌿 Branch: ${{ github.ref_name }}\n🚀 Status: Live"
  #         else
  #           TEXT="❌ *Pipeline Failed*\n📂 Project: ${{ env.PROJECT_TYPE }}\n🌿 Branch: ${{ github.ref_name }}\n💥 Check Logs."
  #         fi
  #         curl -X POST -H 'Content-type: application/json' --data "{\"text\":\"$TEXT\"}" ${{ secrets.SLACK_WEBHOOK }}
